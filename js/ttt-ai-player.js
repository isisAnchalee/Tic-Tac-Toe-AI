(function () {
  if (typeof TTT === "undefined") {
    window.TTT = {};
  }

  var AIPlayer = TTT.AIPlayer = function () {
	  this.game = game;
    this.sign = "o"
  };

  AIPlayer.prototype.makeMove = function(){

  };


})();